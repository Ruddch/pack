# Книга с эффектом перелистывания страниц

React-приложение с реалистичным эффектом перелистывания страниц, как в настоящей книге.

## Возможности

- 🎨 Реалистичный эффект перелистывания страниц
- 🖱️ Перетаскивание угла страницы для перелистывания
- ✨ Автоматическое завершение перелистывания при достижении порога
- 📱 Адаптивный дизайн

## Установка

```bash
npm install
```

## Запуск

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## Использование

1. Наведите курсор на правый верхний угол страницы
2. Зажмите и перетащите угол влево-вверх
3. Если дотяните до половины, страница автоматически перелистнется
4. Если отпустите раньше, страница вернется на место

## Деплой на GitHub Pages

### Автоматический деплой (рекомендуется)

1. Убедитесь, что в `vite.config.js` указан правильный base path (по умолчанию `/cards/`)
2. Если ваш репозиторий называется по-другому, измените base path в `vite.config.js` на `/ваше-имя-репозитория/`
3. В настройках репозитория GitHub перейдите в Settings → Pages
4. В разделе "Source" выберите "GitHub Actions"
5. При каждом push в ветку `main` или `master` проект автоматически задеплоится

### Ручной деплой

```bash
npm run deploy
```

После выполнения команды проект будет доступен по адресу:
`https://ваш-username.github.io/cards/`

**Важно:** Если ваш репозиторий называется не `cards`, измените base path в `vite.config.js`:
```js
base: process.env.NODE_ENV === 'production' ? '/ваше-имя-репозитория/' : '/',
```

## Технологии

- React 18
- Vite
- CSS3 3D Transforms


